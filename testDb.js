const db = require('./models')

const bountyTest = async () => {
    try {
        // CREATE
        // READ
        // UPDATE
        // DESTROY
    } catch(err) {
        console.log(err)
    }
}

bountyTest()